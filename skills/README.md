# Skills

Each subdirectory is one reusable agent skill.

Expected minimum:

```text
skill-name/
└── SKILL.md
```

Optional resources:

- `references/` for longer documentation loaded only when needed.
- `scripts/` for deterministic helper programs.
- `assets/` for templates or media used by the skill.

